# AGENTS.md — brocconi

Deterministic browser harness primitives extracted from ushman: keep this package Bun-first, library-only, and preserve legacy preboot bytes whenever callers pass `globalNamespace: '__ushmanPreboot'`.
