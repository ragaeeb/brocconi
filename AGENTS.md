# AGENTS.md — brocconi

Deterministic browser harness primitives: keep this package Bun-first, library-only, and preserve legacy preboot bytes whenever callers pass `globalNamespace: '__ushmanPreboot'`.
